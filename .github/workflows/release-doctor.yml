name: Release Doctor
on:
  pull_request:
    branches:
      - main
  workflow_dispatch:

jobs:
  release_doctor:
    name: release doctor
    runs-on: ubuntu-latest
    if: github.repository == 'nora-test-account/terraform-provider-nora-test-go-access' && (github.event_name == 'push' || github.event_name == 'workflow_dispatch' || startsWith(github.head_ref, 'release-please') || github.head_ref == 'next')

    steps:
      - uses: actions/checkout@v4

      - name: Check release environment
        run: |
          bash ./bin/check-release-environment
        env:
          GPG_SIGNING_KEY: ${{ secrets.NORA_TEST_PROJECT_REPO_ACCESS_GPG_SIGNING_KEY || secrets.GPG_SIGNING_KEY }}
          GPG_SIGNING_PASSWORD: ${{ secrets.NORA_TEST_PROJECT_REPO_ACCESS_GPG_SIGNING_PASSWORD || secrets.GPG_SIGNING_PASSWORD }}
